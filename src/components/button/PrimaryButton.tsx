import React from 'react'

function PrimaryButton() {
  return (
    <div>
        <button className='py-3 text-center bg-[var(--foreground)] text-[var(--button)] rounded-full mb-5 w-full'>
            Continue
        </button>
    </div>
  )
}

export default PrimaryButton